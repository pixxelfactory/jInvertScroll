/*! jInvertScroll 0.8.3 | Mit License */
(function($){'use strict';$.jInvertScroll=function(sel,options){var defaults={width:'auto',height:'auto',onScroll:function(percent){}};var config=$.extend(defaults,options);if(typeof sel==='Object'&&sel.length>0){return}var elements=[],longest=0,totalHeight,winHeight,winWidth;$.each(sel,function(i,val){$(val).each(function(e){elements.push($(this));var w=$(this).width();if(longest<w){longest=w}})});if(config.width=='auto'){config.width=longest}if(config.height=='auto'){config.height=longest}$('body').css('height',config.height+'px');$([document,window]).on('ready resize',function(e){totalHeight=$(document).height();winHeight=$(window).height();winWidth=$(window).width()});$(window).on('scroll resize',function(e){var currY=$(this).scrollTop();var diff=totalHeight-winHeight;var scrollPercent=0;if(diff!=0){var scrollPercent=(currY/diff).toFixed(4)}if(typeof config.onScroll==='function'){config.onScroll.call(this,scrollPercent)}$.each(elements,function(i,el){var deltaW=el.width()-winWidth;if(deltaW<=0){deltaW=el.width()}var pos=Math.floor(deltaW*scrollPercent)*-1;el.css('left',pos)})})}}(jQuery));